# Leave this empty when the frontend and backend are served from the same origin
# or when Vite dev proxy is forwarding API routes to the backend.
VITE_API_BASE_URL=

# Example for a separately deployed backend:
# VITE_API_BASE_URL=https://api.your-domain.com
